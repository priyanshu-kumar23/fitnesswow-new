export interface Program {
  title: string;
  description: string;
  icon: any;
}

export interface Trainer {
  name: string;
  specialty: string;
  image: string;
}

export interface Pricing {
  title: string;
  price: string;
  features: string[];
}